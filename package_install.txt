pip install django
pip install pymongo
pip install dnspython (use use the mongodb+srv:// URIs)
pip install autopep8 (format code)
pip install pylint (format code)
pip install black (format code)
pip install django-rest-swagger
pip install -U drf-yasg (swagger)
pip install -U drf-yasg[validation] (swagger)